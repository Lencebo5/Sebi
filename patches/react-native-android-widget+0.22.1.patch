diff --git a/node_modules/react-native-android-widget/android/src/main/AndroidManifest.xml b/node_modules/react-native-android-widget/android/src/main/AndroidManifest.xml
index c156499..24edfe1 100644
--- a/node_modules/react-native-android-widget/android/src/main/AndroidManifest.xml
+++ b/node_modules/react-native-android-widget/android/src/main/AndroidManifest.xml
@@ -5,6 +5,7 @@
             android:name=".RNWidgetImageProvider"
             android:authorities="${applicationId}.rnwidget.imageprovider"
             android:exported="true"
+            android:grantUriPermissions="true"
         />
     </application>
 </manifest>
diff --git a/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java b/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java
index 5650d1e..c0bf955 100644
--- a/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java
+++ b/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java
@@ -4,6 +4,8 @@ import android.app.PendingIntent;
 import android.appwidget.AppWidgetManager;
 import android.content.ComponentName;
 import android.content.Intent;
+import android.content.pm.PackageManager;
+import android.content.pm.ResolveInfo;
 import android.graphics.Bitmap;
 import android.graphics.Canvas;
 import android.graphics.Rect;
@@ -74,8 +76,26 @@ public class RNWidget {
         );
 
         Bitmap bitmap = drawViewToBitmap(widgetWithViews.getRootView());
-        Uri bitmapUri = saveBitmapToDisk(widgetId, "light", bitmap);
-        remoteWidgetView.setImageViewUri(R.id.rn_widget_image_light, bitmapUri);
+        // PATCH (Sebi): Samsung One UI's widget host fails to load content://
+        // images set via setImageViewUri and silently discards the whole
+        // RemoteViews update (the widget stays on its previous state /
+        // initial layout). Deliver the bitmap DIRECTLY when it is safely
+        // within the AppWidget bitmap-memory budget (~1.5 × 4 × screen
+        // pixels, enforced by AppWidgetService); fall back to the URI
+        // provider only for oversized bitmaps.
+        boolean direct = shouldUseDirectBitmap(bitmap);
+        android.util.Log.d("SEBI_WIDGET_NATIVE", "drawWidget id=" + widgetId
+            + " view=" + widgetWithViews.getRootView().getMeasuredWidth() + "x" + widgetWithViews.getRootView().getMeasuredHeight()
+            + " bitmap=" + bitmap.getWidth() + "x" + bitmap.getHeight()
+            + " bytes=" + bitmap.getByteCount()
+            + " delivery=" + (direct ? "setImageViewBitmap" : "contentUri"));
+        Uri bitmapUri = null;
+        if (direct) {
+            remoteWidgetView.setImageViewBitmap(R.id.rn_widget_image_light, bitmap);
+        } else {
+            bitmapUri = saveBitmapToDisk(widgetId, "light", bitmap);
+            remoteWidgetView.setImageViewUri(R.id.rn_widget_image_light, bitmapUri);
+        }
 
         if (widgetWithViews.getRootAccessibilityLabel() != null) {
             remoteWidgetView.setContentDescription(R.id.rn_widget_clickable_container, widgetWithViews.getRootAccessibilityLabel());
@@ -90,8 +110,12 @@ public class RNWidget {
             );
 
             Bitmap darkBitmap = drawViewToBitmap(darkWidgetWithViews.getRootView());
-            Uri darkBitmapUri = saveBitmapToDisk(widgetId, "dark", darkBitmap);
-            remoteWidgetView.setImageViewUri(R.id.rn_widget_image_dark, darkBitmapUri);
+            if (direct && shouldUseDirectBitmap(darkBitmap)) {
+                remoteWidgetView.setImageViewBitmap(R.id.rn_widget_image_dark, darkBitmap);
+            } else {
+                Uri darkBitmapUri = saveBitmapToDisk(widgetId, "dark", darkBitmap);
+                remoteWidgetView.setImageViewUri(R.id.rn_widget_image_dark, darkBitmapUri);
+            }
 
             List<CollectionView> lightCollectionViews = widgetWithViews.getCollectionViews();
             List<CollectionView> darkCollectionViews = darkWidgetWithViews.getCollectionViews();
@@ -101,7 +125,13 @@ public class RNWidget {
                 RNWidgetCollectionService.storeCollection(appContext, widgetId, i, darkCollectionViews.get(i).getRenderedViews(), "dark");
             }
         } else {
-            remoteWidgetView.setImageViewUri(R.id.rn_widget_image_dark, bitmapUri);
+            if (direct) {
+                // Same bitmap instance for both modes — RemoteViews' bitmap
+                // cache dedupes identical instances, so it is carried once.
+                remoteWidgetView.setImageViewBitmap(R.id.rn_widget_image_dark, bitmap);
+            } else {
+                remoteWidgetView.setImageViewUri(R.id.rn_widget_image_dark, bitmapUri);
+            }
 
             List<CollectionView> collectionViews = widgetWithViews.getCollectionViews();
             for (int i = 0; i < Math.min(collectionViews.size(), RNWidgetCollectionService.MAX_COLLECTION_WIDGETS); i++) {
@@ -116,8 +146,20 @@ public class RNWidget {
         }
         addCollectionViews(widgetId, remoteWidgetView, widgetWithViews);
 
+        android.util.Log.d("SEBI_WIDGET_NATIVE", "updateAppWidget id=" + widgetId + " applying…");
         AppWidgetManager.getInstance(appContext)
             .updateAppWidget(widgetId, remoteWidgetView);
+        android.util.Log.d("SEBI_WIDGET_NATIVE", "updateAppWidget id=" + widgetId + " applied");
+    }
+
+    // PATCH (Sebi): direct bitmaps are allowed when comfortably below the
+    // AppWidgetService bitmap-memory budget (1.5 × 4 bytes × screen pixels).
+    // We use 50% of that budget as our ceiling; a light+dark pair of
+    // distinct bitmaps therefore stays within the enforced limit.
+    private boolean shouldUseDirectBitmap(Bitmap bitmap) {
+        android.util.DisplayMetrics metrics = appContext.getResources().getDisplayMetrics();
+        long budget = (long) (1.5f * 4 * metrics.widthPixels * metrics.heightPixels);
+        return bitmap.getByteCount() <= budget / 2;
     }
 
     public WritableMap createPreview(int width, int height) throws Exception {
@@ -145,10 +187,60 @@ public class RNWidget {
         return bitmap;
     }
 
-    private Uri saveBitmapToDisk(int widgetId, String mode, Bitmap bitmap) {
+    // PATCH (Sebi): URI fallback path (only used for oversized bitmaps).
+    // Writes and VERIFIES the PNG, grants read access on the exact URI to
+    // the current default HOME launcher, and self-tests that the app itself
+    // can open the URI through the resolver before it is handed to the host.
+    private Uri saveBitmapToDisk(int widgetId, String mode, Bitmap bitmap) throws Exception {
         String fileName = "widget_" + widgetId + "_mode_" + mode + ".png";
-        RNWidgetImageProvider.writeImage(appContext, fileName, bitmap);
-        return RNWidgetImageProvider.getImageUri(appContext, fileName);
+        boolean written = RNWidgetImageProvider.writeImage(appContext, fileName, bitmap);
+        java.io.File file = new java.io.File(
+            new java.io.File(appContext.getFilesDir(), "widget_images"), fileName);
+        android.util.Log.d("SEBI_WIDGET_NATIVE", "png written=" + written
+            + " file=" + file.getPath() + " exists=" + file.exists() + " size=" + file.length());
+        if (!written || !file.exists() || file.length() == 0) {
+            throw new Exception("Widget PNG write failed: " + file.getPath());
+        }
+
+        Uri uri = RNWidgetImageProvider.getImageUri(appContext, fileName);
+        android.util.Log.d("SEBI_WIDGET_NATIVE", "image uri=" + uri);
+
+        grantUriReadToDefaultHome(uri);
+
+        try (android.os.ParcelFileDescriptor pfd =
+                 appContext.getContentResolver().openFileDescriptor(uri, "r")) {
+            android.util.Log.d("SEBI_WIDGET_NATIVE",
+                "self openFileDescriptor ok size=" + (pfd != null ? pfd.getStatSize() : -1));
+        } catch (Exception e) {
+            android.util.Log.e("SEBI_WIDGET_NATIVE", "self openFileDescriptor FAILED", e);
+            throw e;
+        }
+        try (android.database.Cursor cursor =
+                 appContext.getContentResolver().query(uri, null, null, null, null)) {
+            android.util.Log.d("SEBI_WIDGET_NATIVE", "self query rows="
+                + (cursor != null ? cursor.getCount() : -1));
+        } catch (Exception e) {
+            android.util.Log.e("SEBI_WIDGET_NATIVE", "self query FAILED", e);
+        }
+
+        return uri;
+    }
+
+    private void grantUriReadToDefaultHome(Uri uri) {
+        try {
+            Intent homeIntent = new Intent(Intent.ACTION_MAIN).addCategory(Intent.CATEGORY_HOME);
+            ResolveInfo home = appContext.getPackageManager()
+                .resolveActivity(homeIntent, PackageManager.MATCH_DEFAULT_ONLY);
+            if (home != null && home.activityInfo != null) {
+                String homePackage = home.activityInfo.packageName;
+                appContext.grantUriPermission(homePackage, uri, Intent.FLAG_GRANT_READ_URI_PERMISSION);
+                android.util.Log.d("SEBI_WIDGET_NATIVE", "granted read on " + uri + " to " + homePackage);
+            } else {
+                android.util.Log.e("SEBI_WIDGET_NATIVE", "no default HOME launcher resolved");
+            }
+        } catch (Exception e) {
+            android.util.Log.e("SEBI_WIDGET_NATIVE", "grantUriPermission failed", e);
+        }
     }
 
     @RequiresApi(Build.VERSION_CODES.N)
diff --git a/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java b/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java
index 0f963f0..6a793c2 100644
--- a/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java
+++ b/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java
@@ -4,9 +4,11 @@ import android.content.ContentProvider;
 import android.content.ContentValues;
 import android.content.Context;
 import android.database.Cursor;
+import android.database.MatrixCursor;
 import android.graphics.Bitmap;
 import android.net.Uri;
 import android.os.ParcelFileDescriptor;
+import android.provider.OpenableColumns;
 import androidx.annotation.NonNull;
 import androidx.annotation.Nullable;
 
@@ -29,13 +31,15 @@ public class RNWidgetImageProvider extends ContentProvider {
             throw new FileNotFoundException("Write access denied. This provider is Read-Only.");
         }
 
+        // PATCH (Sebi): cacheKey is a REAL query parameter now, so the last
+        // path segment is the clean file name — no regex stripping needed.
         String fileName = uri.getLastPathSegment();
         if (fileName == null) {
             throw new FileNotFoundException("Invalid URI: " + uri);
         }
 
         File folder = getFolderWithImages(getContext());
-        File file = new File(folder, fileName.replaceAll("\\?.*", ""));
+        File file = new File(folder, fileName);
 
         // Verify the file exists and is actually inside our folder (Path Traversal check)
         try {
@@ -54,8 +58,43 @@ public class RNWidgetImageProvider extends ContentProvider {
     }
 
     // --- Boilerplate required for ContentProvider ---
+    // PATCH (Sebi): some OEM AppWidget hosts (notably Samsung's launcher)
+    // query image metadata before opening the stream; returning null makes
+    // them treat the widget image as missing and the widget renders fully
+    // transparent. Answer with a valid OpenableColumns cursor instead.
     @Nullable @Override
-    public Cursor query(@NonNull Uri uri, @Nullable String[] projection, @Nullable String selection, @Nullable String[] selectionArgs, @Nullable String sortOrder) { return null; }
+    public Cursor query(@NonNull Uri uri, @Nullable String[] projection, @Nullable String selection, @Nullable String[] selectionArgs, @Nullable String sortOrder) {
+        String fileName = uri.getLastPathSegment();
+        if (fileName == null) {
+            return null;
+        }
+        File folder = getFolderWithImages(getContext());
+        File file = new File(folder, fileName);
+        try {
+            // Same path-traversal guard as openFile; expose metadata only
+            // for real files inside widget_images.
+            if (!file.getCanonicalPath().startsWith(folder.getCanonicalPath()) || !file.exists()) {
+                return null;
+            }
+        } catch (Exception e) {
+            return null;
+        }
+        String[] columns = projection != null
+            ? projection
+            : new String[]{OpenableColumns.DISPLAY_NAME, OpenableColumns.SIZE};
+        MatrixCursor cursor = new MatrixCursor(columns, 1);
+        MatrixCursor.RowBuilder row = cursor.newRow();
+        for (String column : columns) {
+            if (OpenableColumns.DISPLAY_NAME.equals(column)) {
+                row.add(file.getName());
+            } else if (OpenableColumns.SIZE.equals(column)) {
+                row.add(file.length());
+            } else {
+                row.add(null);
+            }
+        }
+        return cursor;
+    }
     @Nullable @Override
     public String getType(@NonNull Uri uri) { return "image/png"; } // Assume PNGs
     @Nullable @Override
@@ -86,7 +125,9 @@ public class RNWidgetImageProvider extends ContentProvider {
         }
     }
 
-    static void writeImage(Context context, String imageName, Bitmap bitmap) {
+    // PATCH (Sebi): report success, flush before close, and check the
+    // compress() return value so a failed write never goes unnoticed.
+    static boolean writeImage(Context context, String imageName, Bitmap bitmap) {
         File folder = getFolderWithImages(context);
         if (!folder.exists()) folder.mkdirs();
         File file = new File(folder, imageName);
@@ -94,9 +135,12 @@ public class RNWidgetImageProvider extends ContentProvider {
         FileOutputStream fos = null;
         try {
             fos = new FileOutputStream(file);
-            bitmap.compress(Bitmap.CompressFormat.PNG, 100, fos);
+            boolean compressed = bitmap.compress(Bitmap.CompressFormat.PNG, 100, fos);
+            fos.flush();
+            return compressed;
         } catch (Exception e) {
             e.printStackTrace();
+            return false;
         } finally {
             try {
                 if (fos != null) {
@@ -108,14 +152,18 @@ public class RNWidgetImageProvider extends ContentProvider {
         }
     }
 
+    // PATCH (Sebi): canonical URI — cacheKey is a real query parameter
+    // instead of query-like syntax smuggled inside the path segment, which
+    // OEM hosts and resolvers may reject or normalize unpredictably.
     static Uri getImageUri(Context context, String fileName) {
         String authority = context.getPackageName() + AUTHORITY_SUFFIX;
 
         return new Uri.Builder()
             .scheme("content")
             .authority(authority)
-            // Add cacheKey since the uri is cached and returns old images
-            .appendPath(fileName + "?cacheKey=" + System.currentTimeMillis())
+            .appendPath(fileName)
+            // cacheKey busts host-side URI caching between draws
+            .appendQueryParameter("cacheKey", String.valueOf(System.currentTimeMillis()))
             .build();
     }
 
